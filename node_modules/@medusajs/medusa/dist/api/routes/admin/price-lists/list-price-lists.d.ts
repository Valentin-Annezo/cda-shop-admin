import { FilterablePriceListProps } from "../../../../types/price-list";
import { Request } from "express";
declare const _default: (req: Request, res: any) => Promise<void>;
/**
 * @oas [get] /price-lists
 * operationId: "GetPriceLists"
 * summary: "List Price Lists"
 * description: "Retrieves a list of Price Lists."
 * x-authenticated: true
 * tags:
 *   - Price List
 * responses:
 *   200:
 *     description: OK
 *     content:
 *       application/json:
 *         schema:
 *           properties:
 *             price_lists:
 *              type: array
 *              items:
 *                $ref: "#/components/schemas/price_list"
 *             count:
 *               description: The number of Price Lists.
 *               type: integer
 *             offset:
 *               description: The offset of the Price List query.
 *               type: integer
 *             limit:
 *               description: The limit of the Price List query.
 *               type: integer
 */
export default _default;
export declare class AdminGetPriceListPaginationParams extends FilterablePriceListProps {
    offset?: number;
    limit?: number;
    expand?: string;
    order?: string;
}
