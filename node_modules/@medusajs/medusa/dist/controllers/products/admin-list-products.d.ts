import { AwilixContainer } from "awilix";
import { AdminProductsListRes } from "../../api";
import { Product } from "../../models/product";
import { FilterableProductProps } from "../../types/product";
declare type ListContext = {
    limit: number;
    offset: number;
    order?: string;
    fields?: string;
    expand?: string;
    allowedFields?: string[];
    defaultFields?: (keyof Product)[];
    defaultRelations?: string[];
};
declare const listAndCount: (scope: AwilixContainer, query: FilterableProductProps, body?: object | undefined, context?: ListContext) => Promise<AdminProductsListRes>;
export default listAndCount;
