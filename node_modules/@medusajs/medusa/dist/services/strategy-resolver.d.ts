import { AbstractBatchJobStrategy, TransactionBaseService } from "../interfaces";
import { EntityManager } from "typeorm";
declare type InjectedDependencies = {
    manager: EntityManager;
    [key: string]: unknown;
};
export default class StrategyResolver extends TransactionBaseService<StrategyResolver, InjectedDependencies> {
    protected manager_: EntityManager;
    protected transactionManager_: EntityManager | undefined;
    constructor(container: InjectedDependencies);
    resolveBatchJobByType<T extends TransactionBaseService<never>>(type: string): AbstractBatchJobStrategy<T>;
}
export {};
